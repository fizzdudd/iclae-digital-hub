# Copia este archivo a .env y completa los valores reales.
# El .env NO se sube a git (está en .gitignore). Pide la contraseña real al encargado.

DB_NAME=iclae
DB_USER=iclae
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
